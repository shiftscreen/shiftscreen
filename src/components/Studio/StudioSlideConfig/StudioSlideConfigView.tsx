import React from 'react';

import { Container } from './StudioSlideConfigStyle';

interface Props {

}

const StudioSlideConfig: React.FC<Props> = (props: Props) => {
  return (
    <Container>

    </Container>
  );
};

export default StudioSlideConfig;
