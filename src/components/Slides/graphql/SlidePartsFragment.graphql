fragment BasicSlideParts on Slide {
    id
    createdAt
    updatedAt
    date
    durationSeconds
    isActive
    time
    transition
    weekdays

    appInstance {
        ...BasicAppInstanceParts
    }
}
