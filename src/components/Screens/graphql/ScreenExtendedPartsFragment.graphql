fragment ExtendedScreenParts on Screen {
    ...BasicScreenParts

    viewerRole {
        ...BasicRoleParts
    }

    slides {
        ...BasicSlideParts
    }

    organization {
        ...BasicOrganizationParts
    }
}
