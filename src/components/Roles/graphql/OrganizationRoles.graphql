query OrganizationRoles($organizationId: Int!) {
    organization(id: $organizationId) {
        ...BasicOrganizationParts
        roles {
            ...BasicRolesParts,
            user {
                ...BasicUserParts
            }
        }
    }
}
